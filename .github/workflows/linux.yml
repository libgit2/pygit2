name: Linux

on:
  pull_request:
  push:

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-20.04, macos-latest]
        python-version: [3.6, 3.9, pypy3]
        exclude:
        - os: macos-latest
          python-version: 3.9
        - os: macos-latest
          python-version: pypy3

    steps:
    - name: Checkout pygit2
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: ${{ matrix.python-version }}

    - name: Linux
      if: runner.os == 'Linux'
      run: |
        LIBSSH2_VERSION=1.9.0 /bin/sh ci.sh

    - name: macOS
      if: runner.os == 'macOS'
      run: |
        brew install libssh2
        /bin/sh ci.sh
